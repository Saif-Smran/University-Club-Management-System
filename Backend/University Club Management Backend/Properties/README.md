# ⚙️ Project Launch Properties (`Properties/`)

This directory contains configuration profiles used by ASP.NET Core when launching the application in development mode.

---

## 📂 Files

- **`launchSettings.json`**: Defines server launch profiles (HTTP/HTTPS ports, environment variables, IIS Express / Kestrel settings) when running `dotnet run` or executing from Visual Studio / VS Code.
